@model Movie

<div class="film">
    @*  <p>@ViewBag.FilmBasligi</p>
    <p>@ViewBag.FilmAciklamasi</p>
    <p>@ViewBag.FilmYonetmen</p>
    <p>
    <ul>
    @foreach (var item in ViewBag.Oyuncular)
    {
    <li>@item</li>
    }
    </ul>
    </p> *@

    <div class="card">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-4">
                    <img class="card-img-top" src="/img/@Model.ImageUrl" alt="@Model.Title">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">@Model.Title</h5>
                        <div>
                            <a class="btn btn-primary"  asp-controller="Movies" asp-action="details" asp-route-id="@Model.MovieId">Detils</a>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>